import { Config } from '../../index.ts';

export default function authorizationCodeFlow({
  client_id,
  client_secret,
  grant_type,
  response_type,
  scope,
  redirect_uri,
  token_uri,
  auth_uri,
}: Config) {

}
